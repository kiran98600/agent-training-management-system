(() => {
    const profileRepository = require('./branches.repository');
    module.exports = {
        // getMenus: profileRepository.getMenus(),
        // getSubMenus: profileRepository.getMenus(),
        // getActions: profileRepository.getActions()
    }
})();
